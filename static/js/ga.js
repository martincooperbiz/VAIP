// In your JavaScript code, you can use the dotenv library to load the environment variables from the .env file.
const dotenv = require('dotenv');
dotenv.config();

// Now you can access your environment variables in your JavaScript code.
const GA_ID = process.env.GA_ID;

